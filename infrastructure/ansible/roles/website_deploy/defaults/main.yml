# Env file for reading by docker to know where secrets are stored
# remote_env_file: /tmp/.env
# Path for cert-FILE locally and on vm(same path)
sa_certificate_key_file: "{{ secret_ops_dir }}/yc_cert_downloader_sa_key.json"
# Path for cert-DIR inside container