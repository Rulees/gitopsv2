# /etc/nginx/conf.d/*.conf
server {
    listen ${PORT};
    server_name _;

    root /sites/starter;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    ###
    # Other
    ###
}
   


